<html>
  <head>
  </head>
  <body>
    <h1>Bienvenue sur le best site de gossip ever !</h1>
    <h3 class='text-center'>Jeremstar en PLS</h3>
    <p>Voici la liste de tous nos gossips:</p>
    <div class="d-flex flex-wrap justify-content-center">
      <% @all_gossips.each_with_index do |gossip, index| %>
        <div class="card my-2 mx-4" style="width: 18rem;">
          <div class="card-header">
            Gossip numéro <%=index + 1%>
          </div>
          <div class="card-body">
            <h5 class="card-title">Titre : <%= gossip.title%></h5>
            <p class="card-text">Ecrit par <%=gossip.user.first_name%></p>
            <%=link_to "En voir plus", "gossips/#{gossip.id}", class:"btn btn-secondary" %>
          </div>
        </div>
      <% end %>
    </div>
  </body>
</html>